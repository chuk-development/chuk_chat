# test/assistant · Signatures

## test/assistant/assistant_microphone_test.dart  (153 Z.)

- L10 `Uint8List _chunk({required double amplitude, Duration length = _frame})`  — One chunk of PCM16 at the given amplitude (0..1), as a 400 Hz tone so the
- L25 `_frame = Duration(milliseconds: 32)`
- L27 `void _feed( AssistantMicrophone mic, { required double amplitude, required Duration total, })`
- L38 `void main()`

## test/assistant/assistant_overlay_test.dart  (297 Z.)

- L9 `_accent = Color(0xFFFF7043)`
- L10 `_bg = Color(0xFF1A1113)`
- L12 `Widget _host(Widget child)`
- L22 `void main()`

## test/assistant/assistant_tools_test.dart  (145 Z.)

- L6 `void main()`
