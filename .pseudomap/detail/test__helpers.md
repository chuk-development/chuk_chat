# test/helpers · Signatures

## test/helpers/icon_finder.dart  (25 Z.)

- L15 `Finder findIcon(IconData icon)`  — Finds an icon whichever widget draws it.
- L21 `Finder findWidgetWithIcon(Type type, IconData icon)`  — [find.widgetWithIcon] for the same reason.
