// lib/utils/certificate_pinning_register_stub.dart
//
// Web stub — certificate pinning registration is a no-op.
// The browser handles TLS certificate validation.

/// No-op on web. The browser's TLS stack validates certificates.
void registerCertificatePinning() {
  // No-op on web
}
