/// Web: there is no file system to read a coworker's picture from, so the face
/// always falls back to the monogram.
library;

import 'package:flutter/widgets.dart';

ImageProvider<Object>? faceImageProvider(String? path) => null;
